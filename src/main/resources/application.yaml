## WebServer

server:
  servlet:
    context-path: /api 

## Datasource

spring:
  h2: 
    console:
      enabled: true
  datasource:
    url:  jdbc:h2:mem:testdb
    driveClassName:  org.h2.Driver
    username: root 
    password: root 
  jpa:
    database-platform:  
